import { render } from '@testing-library/react'

import { UseHooksPage } from '../use-hook'

describe('<UseHooksPage/>', () => {
  it('should render list', () => {
    render(<UseHooksPage />)
  })
})
